export type Theme = {
  mode: "light" | "dark";
  background: string;
  text: string;
  primary: string;
  primaryText: string;
  card: string;
  border: string;
  inputBackground: string;
  placeholder: string;
};